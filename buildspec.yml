# buildspec.yml
version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
  build:
    commands:
      - npm run build
      - npm run export   # ← ★ これを追加！
artifacts:
  files:
    - '**/*'
  base-directory: out
